{
  "dependencies": {
    "cors": "^2.8.5",
    "json-server": "^0.17.3",
    "json-server-auth": "^2.1.0"
  },
  "name": "db",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "nodemon main.js",
    "server": "json-server --watch db.json --port 4000 --no-cors"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "nodemon": "^2.0.22"
  }
}
